venv/
# Whole folder, not just models/*.pt: the SpeciesNet weights live in a subfolder and
# slipped past the narrower pattern, staging ~500MB of binaries GitHub would reject.
models/
*.exe
build/
dist/
*.spec
work/
work_card/
test_subset/
test_output/
test_work/
results/
__pycache__/
*.pyc
